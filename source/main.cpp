#include <iostream>
#include "../headers/macros.h"
#include "../headers/NetworkPlatformCheck.h"

using namespace std;

int main() {
	
	SOCKET udpSocket = socket(AF_INET, SOCK_DGRAM, 0);
	//SOCKET tcpSocket = socket(AF_INET6, SOCK_STREAM, 0);
	


	
	
	return 0;
}